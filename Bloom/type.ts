// This file defines the "shape" of our data.

// Defines what a Recording object looks like
export interface Recording {
    uri: string;
    date: string;
    day: string;
    formattedDate: string;
  }